1.docker run -d -p 5000:5000 --net=host --restart=always --name docker-registry registry
   
  Note: images are stored in /var/lib/registry/docker/registry/v2/repositories
  
2. config docker deamon to connect to the private registry via /etc/sysconfig/docker
   eg:
      OPTIONS="-g /opt/scs/docker --insecure-registry 10.11.150.76:5000"
      
<<<<<<<<<<<<<<<<< docker registry backend storage >>>>>>>>>>>>>>>>>>>>>>
- ceph swift
    

- 

<<<<<<<<<<< how to use local docker registry to push/pull images >>>>>>>>
- pull images from local docker registry:
  docker pull docker-registry:5000/alpine
- tag images:
  docker tag alpine:latest docke-registry:5000/alpine:latest
- push images to local docker registry:
  docker push docker-registry:5000/alpine:latest  
- search images from local registry:
  docker search docker-registry:5000/alpine

Reference:
- 
